https://github.com/puneetgaur2006/udacity


Reviewer Note
You resubmitted with notebooks (Udaplay_01 and Udaplay_02), a games/ folder with multiple JSON files, three tools (retrieve, evaluate, web search), an LLM (Ollama Mistral) for evaluation, and Tavily for web search. Run outputs are present and the flow (retrieve → evaluate → answer or web search) works. 
Thank you for addressing the previous feedback in part.

The assignment, however, requires you to build on the course-provided starter notebooks 
and the provided lib/ (state machine, agents, tooling). 
Your submission still uses a custom implementation that does not use the starter structure,
 the course ChromaDB format, the course agent/tool framework, 
 or the required session and logging behavior. 
 The rubric also states that hard-coding the tool execution order
  (e.g. a fixed RETRIEVE → EVALUATE → ANSWER/WEB_SEARCH loop) is not acceptable;
   the agent must use the course framework so that the LLM decides when to call which tool. 
Below is feedback by rubric section and what you need to change.

Summary: what to do next
Start from the course starter
Use the provided Udaplay_01_starter_project.ipynb and Udaplay_02_starter_project.ipynb and the lib/ (state_machine, agents, tooling). 
Implement the three tools with @tool and lib.agents.Agent so that tool selection is LLM-driven, not hard-coded.

Use persistent ChromaDB
In Part 01, use chromadb.PersistentClient(path="chromadb") 
and the same client/collection in Part 02 for retrieve_game.

Make tool names visible
Ensure the run output shows which tools were called by name (retrieve_game, evaluate_retrieval, game_web_search).

Use explicit session_id
In the demo, call agent.invoke(query, session_id="udaplay_demo") (or similar) for the queries that belong to the same session.

Use the course-provided games data
Do not replace the provided games/ folder with your own files or schema. Use the course-provided
game JSON files (e.g. 001.json–015.json) and 
the schema (Name, Platform, Genre, Publisher, Description, YearOfRelease).
---------------------------------------------------------------------------
RAG

Prepare and process a local dataset of video game information for use in a vector database and RAG pipeline

Reviewer Note

What must change

The assignment requires the course-provided games data: the provided games/ folder (e.g. 001.json through 015.json) with the specified schema (Name, Platform, Genre, Publisher, Description, YearOfRelease). You replaced this with your own data: 5 custom files (fifa21.json, gow_ragnarok.json, pubg.json, road_rash.json, tekken7.json) and a different schema (title, release_date, platforms, publisher, description). You must not substitute your own game data. Use the data provided by the course.
The rubric requires a persistent vector database. You use chromadb.Client() with no path, so the database is in-memory only. You must use chromadb.PersistentClient(path="chromadb") (or similar) in Part 01 and the same client/collection in Part 02 for the retrieve_game tool.
Note: For your next submission, please include the games folder and the created chromadb folder as part of your deliverables. This will provide clear evidence that your RAG implementation is functioning correctly and help reviewers verify that the vector database setup meets the rubric requirements.


Agent Development

Demonstrate and report on the agent’s performance with example queries.

Reviewer Note

What must change

The rubric requires that the output for each query includes the agent’s reasoning, tool usage, and final answer. As noted above, “tool usage” must show the tool names (retrieve_game, evaluate_retrieval, game_web_search) in the logs, not only state labels. 
Once you use the course agent and tool framework and add proper logging, the same run outputs will satisfy this requirement.

Implement agent tools for internal retrieval, evaluation and websearch fallback
Reviewer Note

What must change

Use the course framework
The assignment expects you to use the course-provided lib/ (e.g. lib.agents.Agent, lib.tooling with the @tool decorator). Your tools are plain functions and your agent is a custom UdaPlayAgent class. You need to:

Define the three tools as @tool-decorated functions (retrieve_game, evaluate_retrieval, game_web_search) as specified in the instructions,
Register them with lib.agents.Agent so that the LLM chooses when to call each tool, instead of a fixed sequence in code.
No hard-coded workflow
Your UdaPlayAgent.run() uses a fixed while loop (RETRIEVE → EVALUATE → ANSWER or WEB_SEARCH). The required design is: the LLM/agent decides which tool to call next (using the state machine and tools from the course), not a predetermined pipeline in your code.

Persistent ChromaDB for retrieve_game
As above, retrieve_game must use the same PersistentClient and collection created in Part 01, not an ephemeral Client().

Tool names visible in output
The rubric requires that the output for each query includes the agent’s reasoning, tool usage, and final answer. “Tool usage” means the names of the tools (retrieve_game, evaluate_retrieval, game_web_search) must appear in the run output. Your output only shows labels like "STATE: RETRIEVE", "STATE: EVALUATE", "STATE: WEB SEARCH". You need to ensure the actual tool names are visible (e.g. in state machine logs or in tool_calls logging when using lib.agents.Agent). See the course materials and the starter solution for how tool execution is logged.

The submission includes at least three tools:
A tool to retrieve game information from the vector database.
A tool to evaluate the quality of retrieved results.
A tool to perform web search using an API (e.g., Tavily).
Each tool is implemented as a function/class and is integrated into the agent workflow.
The agent: 
first attempts to answer using internal knowledge,
evaluates the result,
and falls back to web search if needed.

Build a stateful agent that manages conversation and toolusage

What must change

Use the course agent and state machine
The assignment expects the workflow to be implemented with the course state machine (or equivalent) and lib.agents.Agent, not a custom loop. 
The agent should be invoked with the course API (e.g. agent.invoke(...)).

Explicit session_id
The rubric requires that the agent be invoked with an explicit session_id so that multiple queries in the same session share context. For example:

agent.invoke(query, session_id="udaplay_demo") Your code uses agent.run(question) with no session_id. You need to use the course agent’s invoke method with a session_id parameter (e.g. the same session_id for all demo queries in one session). See the course materials and Knowledge 1072667 for session management.
The agent is implemented as a class or function that maintains conversation state.
The agent can handle multiple queries in a session, remembering previous context.
The agent’s workflow is implemented as a state machine or similar abstraction.
The agent produces clear, structured, and well-cited answers.
